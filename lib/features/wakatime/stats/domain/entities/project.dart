class Project {
  final String name;
  final DateTime totalTimeSpent;
  final DateTime timeSpentToday;

  const Project({
    required this.name,
    required this.totalTimeSpent,
    required this.timeSpentToday,
  });
}
